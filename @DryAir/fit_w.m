function [fitresult, gof] = fit_w(p, T, w)
%CREATEFIT(P,T,W)
%  Create a fit.
%
%  Data for 'w(p,T)' fit:
%      X Input: p
%      Y Input: T
%      Z Output: w
%  Output:
%      fitresult : a fit object representing the fit.
%      gof : structure with goodness-of fit info.
%
%  See also FIT, CFIT, SFIT.

%  Auto-generated by MATLAB on 29-Mar-2023 13:06:25


%% Fit: 'w(p,T)'.
[xData, yData, zData] = prepareSurfaceData( p, T, w );

% Set up fittype and options.
ft = 'linearinterp';
opts = fitoptions( 'Method', 'LinearInterpolant' );
opts.ExtrapolationMethod = 'linear';
opts.Normalize = 'on';

% Fit model to data.
[fitresult, gof] = fit( [xData, yData], zData, ft, opts );

% Plot fit with data.
figure( 'Name', 'w(p,T)' );
h = plot( fitresult, [xData, yData], zData );
legend( h, 'w(p,T)', 'w vs. p, T', 'Location', 'NorthEast', 'Interpreter', 'none' );
% Label axes
xlabel( 'p', 'Interpreter', 'none' );
ylabel( 'T', 'Interpreter', 'none' );
zlabel( 'w', 'Interpreter', 'none' );
grid on


